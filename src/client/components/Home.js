import React, { Component } from "react";

const Home = () => (
  <div>
    <h2>I'm the VERY VERY BSET home component</h2>
    <button onClick={() => console.log("clicked")}>Click Me</button>
  </div>
);

export default Home;
